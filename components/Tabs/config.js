import { colours } from '../common/styles';

export const config = {
    tabBarOptions: {
        activeTintColor: colours.primaryBackground,
        inactiveTintColor: colours.primaryButtonBackground
    },
    tabBarPosition: 'bottom'
};